# ============================================================
# UMGEBUNGSVARIABLEN - Film-API
# ============================================================
# 
# Diese Datei ist eine VORLAGE. Kopiere sie zu ".env" und trage
# die echten Werte ein:
#
#   cp .env.example .env
#
# WICHTIG: Die .env Datei wird NICHT in Git eingecheckt!
# ============================================================

# ------------------------------------------------------------
# DATENBANK (PostgreSQL)
# ------------------------------------------------------------
DATABASE_URL="postgresql://postgres:p@localhost:5432/postgres?schema=film"
POSTGRES_USER=postgres
POSTGRES_PASSWORD=p
POSTGRES_DB=postgres

# ------------------------------------------------------------
# NODE UMGEBUNG
# ------------------------------------------------------------
# Optionen: development, production, test
NODE_ENV=development

# Server Port (Standard: 3000)
PORT=3000

# ------------------------------------------------------------
# CORS (Cross-Origin Resource Sharing)
# ------------------------------------------------------------
# Erlaubte Origins für Frontend-Zugriff
# Beispiel: http://localhost:4200,https://meine-app.de
CORS_ORIGIN=*

# ------------------------------------------------------------
# KEYCLOAK (Authentifizierung)
# ------------------------------------------------------------
KEYCLOAK_URL=http://localhost:8080
KEYCLOAK_REALM=film
KEYCLOAK_CLIENT_ID=film-api

# Keycloak Admin-Credentials (nur für Docker)
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=admin

# ------------------------------------------------------------
# PGADMIN (nur Development)
# ------------------------------------------------------------
PGADMIN_EMAIL=admin@example.com
PGADMIN_PASSWORD=admin
